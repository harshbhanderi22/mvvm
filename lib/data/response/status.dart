/// This code is defining an enumeration type called `status` with three possible values: `LOADING`,
/// `COMPLETED`, and `ERROR`. This can be used to represent the current state of a process or operation
/// in a more readable and type-safe way.
enum Status { LOADING, COMPLETED, ERROR }
